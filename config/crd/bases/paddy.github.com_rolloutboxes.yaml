---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: rolloutboxes.paddy.github.com
spec:
  group: paddy.github.com
  names:
    kind: RolloutBox
    listKind: RolloutBoxList
    plural: rolloutboxes
    singular: rolloutbox
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: RolloutBox is the Schema for the rolloutboxes API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RolloutBoxSpec defines the desired state of RolloutBox
            properties:
              namespace:
                description: Namespace of resource in Kubernetes,includes deployment\service\scalers
                type: string
              service:
                description: Service of kubernetes
                properties:
                  name:
                    description: Name of Kubernetes service generated by paddy, default
                      same with target
                    type: string
                  port:
                    description: Port of service
                    format: int32
                    type: integer
                  portName:
                    type: string
                  targetPort:
                    type: string
                required:
                - port
                - portName
                - targetPort
                type: object
              targetRef:
                properties:
                  apiVersion:
                    description: APIVersion
                    type: string
                  kind:
                    description: Kind of referent
                    type: string
                  name:
                    description: Name of the referent
                    type: string
                required:
                - apiVersion
                - kind
                - name
                type: object
              workInstance:
                description: WorkInstance defined which paddy instance to work with
                  this rollout box
                type: string
            required:
            - namespace
            - service
            - targetRef
            type: object
          status:
            description: RolloutBoxStatus defines the observed state of RolloutBox
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
